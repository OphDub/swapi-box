/* eslint-disable */
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

describe('APP', () => {
  it('should match the snapshot', () => {

  });

  it('should have a default state with an empty film object and empty arrays for planets, people, vehicles, and favorites', () => {

  });

  it('should choose a random object from an array upon mounting', () => {

  });

  it('should be able to getData', () => {

  });

  it('should save objects to the favorites array', () => {

  });

  it('should remove objects from the favorites array', () => {

  });
});
